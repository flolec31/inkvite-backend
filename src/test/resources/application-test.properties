# Plays schema.sql
spring.sql.init.mode=always

# s3 mock configuration
s3.fileUrlFormat=https://test-s3/%s/%s

# mail mock configuration
spring.mail.host=test-mail-host
spring.mail.port=1234
spring.mail.username=test-mail-username
spring.mail.password=test-mail-password

# assets mock
inkvite.logo.url=test-logo-url.test